<div id="end" class="container">
	{{#if h1}}
	<p id="end-txt">
		{{#each h1}}
		<span id="end-txt{{int @index}}"{{#if class}} class="{{class}}"{{/if}}>{{{txt}}}</span>				
		{{/each}}
	</p>
	{{/if}}
	{{#if (isModMobile root)}}
	<div id="end-lockup" class="container">
	{{/if}}
	<img id="end-mag-logo" class="center" src="images/{{root.size.endMagLogo}}" alt="MAGNOLIA HOME"/>
	<div id="end-line" class="line center" aria-hidden="true"></div>
	<svg id="end-jh-logo" class="center" xmlns="http://www.w3.org/2000/svg"><use href="#jh-logo"/></svg>
	{{#if (isModMobile root)}}
	</div>
	{{/if}}
	<div id="cta-container" class="brandon-b cta-txt">
		<p id="cta1" class="brandon-b cta-txt">{{{getCta1Txt root}}}</p>{{!log (getCta1Txt root) }}
		<p id="cta2" class="brandon-b cta-txt">{{{getCta2Txt root}}}</p>
	</div>
</div>
