(function(window, document){
	var tl, replay;

	{{>utilities.js}}

	function init() {
		tl = gsap.timeline({ defaults:{ duration:0.35, ease:'power1.out' }});// GSAP 3.0!
		initReplay();
		animate();
	}	

	{{> (getBannerTimelineJs)}}

	{{> replay.js}}

	window.addEventListener('load', init);

})(this,document);
